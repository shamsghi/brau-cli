[package]
name = "brau"
version = "2.2.0"
edition = "2021"
description = "Fuzzy Homebrew search and install CLI for formulae and casks"
readme = "README.md"
license = "MIT"
rust-version = "1.74"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
